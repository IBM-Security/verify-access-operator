# Copyright contributors to the IBM Verify Identity Access Operator project

resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: localhost:32000/verify-access-operator
  newTag: dev-20260525-083948
